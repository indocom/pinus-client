import React, { useState } from "react";

import { getPersons } from "src/utils/contentful/kudo";

import { Text, Button, Input, TextArea } from "pinus-ui-library";

const SanWriteContent: React.FC = () => {
  return (
    <div>
      <div>asdfasdfa</div>
    </div>
  );
};

export default SanWriteContent;
